import {jobInput, nameInput, userJob, userName} from "../utils/constants.js";

export class UserInfo {
  constructor(name, job) {
    this._name = userName.value;
    this._job = userJob.value
  }

  getUserInfo () {

  }

  setUserInfo () {

  }
}

